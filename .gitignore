# apm package cache (only present if this repo gains its own dependencies)
apm_modules/

# harness output dirs, in case `apm install` is ever run inside this repo
.claude/
.github/copilot/
.agents/

# compiled agent context (generated by `apm compile` from .apm/)
AGENTS.md
CLAUDE.md

# macOS
.DS_Store
